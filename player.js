/*
Player():
    Constructs a new Player object
INPUT:
    node: The HTML element associated with the Player
    model: The Model object that Player is a part of
OUTPUT:
    Returns a new instance of a Player object
*/
export function Player(node, model) {
    this.node = node; 

    let move = function(direction) {
        switch (direction) {
            case direction.UP:
                
        }
    }

}1